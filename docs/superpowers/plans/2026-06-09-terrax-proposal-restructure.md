# 研究方案重构 · 实施计划（Implementation Plan）

> **For agentic workers:** 本计划用于把散乱的 `proposal.md` 重写为结构清晰的 `proposal_v2.md`。可用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务执行。步骤用 `- [ ]` 复选框跟踪。
>
> **适配说明（写作而非写代码）**：本任务是撰写中文学位论文研究方案，没有可运行的单元测试。每个任务的"验证"步骤＝**对照 spec 契约自检**：① 写作约定（漏斗/排比/推导桥/不跳步，见 spec §3）；② RQ↔方法↔创新↔实验 对齐表（spec §5）；③ 去重（同一概念全文仅一处权威定义）。

**Goal:** 以 `20260608_Paper_Thoughts.docx` 的漏斗脉络为骨架，将 `proposal.md` 的方法与实验素材重组、去重、话语统一，产出 `proposal_v2.md`。

**Architecture:** 单文档（`proposal_v2.md`，项目根目录，与 `proposal.md` 并列便于 diff）。六章结构（spec §4）。Task 1 先建标题＋全章 heading 骨架，后续任务用 Edit 逐章填充并各自提交。

**Tech Stack:** Markdown 撰写；素材来源＝`proposal.md`（行号锚点见各任务）＋ docx 抽取文本（引言）＋ `20260602_Papers/files/` 8 篇 PDF（仅"三、现状"需要，先精读）。权威契约＝`docs/superpowers/specs/2026-06-09-terrax-proposal-restructure-design.md`。

**写作铁律（每个任务都适用，源自 spec §3）：**
- 引出问题处按"宽→窄"漏斗显式递进，不直接抛结论。
- 抽象结论用排比拆成读者可逐一点头的并列项；docx 已有排比处**逐字保留**。
- 显式写 because→therefore 过渡句，宁多写连接句也不跳步。
- 术语统一到 docx 体系（语义约束驱动 / 自然语言–空间操作语义对齐 / 面向智能体的数据结构 vs 面向人类文档）。
- **不杜撰**：现状章的文献论断必须来自真实精读（Task 6a），不得凭空造引用。

---

## 文件结构

| 文件 | 责任 | 任务 |
|---|---|---|
| `proposal_v2.md` | 重构后的研究方案正文（六章） | Task 1–5、6b、7 |
| `docs/superpowers/notes/2026-06-09-lit-notes.md` | 8 篇文献精读笔记（现状章的事实来源） | Task 6a |

> 为何单文件：研究方案是连续通读、最终整体转 docx 的文档，拆成多文件会割裂阅读与转换。按章节用 Edit 增量填充即可，提交粒度＝每章一次。

---

## Task 1：建文档骨架 + 第一章「研究背景与问题」

**Files:**
- Create: `proposal_v2.md`

**素材来源：** `docs/superpowers/notes/2026-06-09-docx-intro-extracted.md`（docx 引言 6 段全文，已抽取存档，含"段N→小节"标注）；映射见 spec §4「一」。

- [ ] **Step 1：创建文件，写入标题 + 六章 heading 骨架**

写入以下结构（章标题用中文数字，子节用阿拉伯数字；先只放 heading，正文留待各任务填）：

```markdown
# 面向城市空间分析智能体的语义约束驱动流程规划方法研究

## 一、研究背景与问题
### 1.1 背景：空间分析的价值与传统范式的局限
### 1.2 机遇：大模型驱动的空间分析
### 1.3 问题①：端到端生成在复杂任务中的失效
### 1.4 问题②：空间规划的特殊性与现有修补机制的不足
### 1.5 研究问题凝练与方法总思路
### 1.6 研究意义

## 二、研究目标与研究问题
### 2.1 总目标
### 2.2 研究问题
### 2.3 论点与假设

## 三、国内外研究现状
### 3.1 GeoAI：深度学习与地理信息科学的结合
### 3.2 LLM4GIS：大模型对地理信息科学的影响
### 3.3 Autonomous GIS：地理空间分析自动化之后
### 3.4 规划层与错误处理评述

## 四、研究内容与技术路线
### 4.1 总体设计
### 4.2 L1 规划知识层
### 4.3 L2 任务规划层（语义约束→DAG 两步法）
### 4.4 L3 执行代理层
### 4.5 L4 规划迭代优化层

## 五、实验设计与验证
### 5.1 数据条件
### 5.2 资源库
### 5.3 测试任务集
### 5.4 评估指标
### 5.5 对比基线与消融
### 5.6 实验日志
### 5.7 预期结果

## 六、创新点与预期成果

## 参考文献
```

- [ ] **Step 2：填充第一章 1.1–1.6**

把 `notes/2026-06-09-docx-intro-extracted.md` 的六段引言按其中"段N→小节"标注逐段落填入 1.1–1.6（该文本即权威 prose，仅做切分与子标题衔接，**不改写论证**）。务必保留两处硬要素：
  - **1.3** 逐字保留七项排比：「空间对象遗漏、空间关系误解、约束条件缺失、工具选择错误、步骤顺序不合理、中间数据依赖断裂以及幻觉工具调用」。
  - **1.4** 必须同时包含：①「即使引入审查机制（Reviewer）对执行错误进行后验修补也未能准确映射于规划层」；②「规划结果本质上是面向人类的说明文档，而非面向智能体的数据结构」。
  - **1.5** 必须出现分层主线短语：「自然语言任务 → 空间语义约束 → 工作流图 → 工具流」。

- [ ] **Step 3：验证（对照 spec 契约）**
  - 漏斗 宽→窄 完整（背景→机遇→问题①→问题②→思路→意义）；
  - 七项排比逐字在位；1.4 两要素在位；1.5 四层主线短语在位；
  - 全章术语为 docx 体系。

- [ ] **Step 4：提交**

```bash
git add proposal_v2.md
git commit -m "docs: 重构稿第一章 研究背景与问题（采用docx漏斗）"
```

---

## Task 2：第二章「研究目标与研究问题」

**Files:** Modify `proposal_v2.md`（填充「二」）
**素材来源：** `proposal.md:5-15`（RQ）、`proposal.md:17-26`（假设）；改写口径见 spec §4「二」、§2 决策表。

- [ ] **Step 1：写 2.1 总目标**

一段：构建面向城市空间分析智能体的**语义约束驱动分层规划方法**，将用户的目标导向型自然语言需求经「空间语义约束→工作流图」逐层结构化，并据此实现可验证的执行与基于结构化表示的错误定向修正。

- [ ] **Step 2：写 2.2 三个 RQ（逐字采用 spec §4「二」措辞）**

```markdown
- **RQ1（知识）**：如何构建面向任务规划的知识表示框架，统一存储 GIS 工具的功能特征、参数、环境依赖与兼容性约束？
- **RQ2（规划·核心）**：如何设计多因素约束下「自然语言→空间语义约束→DAG」的两步结构化建模方法，将用户目标自动转换为包含数据流依赖、顺序约束、可直接验证的结构化执行计划？
- **RQ3（验证·反转）**：如何利用语义约束与 DAG 的可映射性，将执行失败精准回溯到规划层缺陷并定向修正，实现规划的自验证与自修正？
```

- [ ] **Step 3：写 2.3 假设（三条，措辞统一；假设3 按反转改写）**
  - 假设1：将公认复杂任务从通用时空任务中独立、构建领域专门化知识表示框架（两类：模式明确操作复杂 / 结论开放操作复杂），能显著提升任务分解的合理性与完整性。
  - 假设2：将规划输出从自然语言文档改为「**空间语义约束 + 有向无环图**」并配显式依赖约束，能有效减少执行阶段的依赖错误与顺序错误；通用大模型无法独立完成空间分析建模的结构化，必须借助外部知识与语义约束中间层。
  - 假设3（**反转**）：正因语义约束＋DAG 提供了可被机器映射的结构，执行失败才能精准回溯到规划层缺陷（语义约束缺失 / DAG 依赖缺失 / 工具抽象不匹配），从而定向修正、显著提升复杂任务 1–2 次执行成功率。

- [ ] **Step 4：验证** — RQ 措辞与 spec 逐字一致；假设3 体现"回溯/定向修正"而非泛化闭环；与 §5 对齐表 RQ 行对得上。
- [ ] **Step 5：提交** `git commit -m "docs: 重构稿第二章 研究目标与研究问题（RQ3反转）"`

---

## Task 3a：第四章 4.1 总体设计 + 4.2 L1 规划知识层

**Files:** Modify `proposal_v2.md`（填充 4.1、4.2）
**素材来源：** `proposal.md:55-75`（总体设计表、3.2.1 L1）。

- [ ] **Step 1：写 4.1 总体设计** — 放 L1–L4 架构表（4 行，逐字取 `proposal.md:57-62`），并补一句过渡：模块分层（L1–L4）与 docx 数据流水线（NL→语义约束→图→工具流）正交，流水线嵌于 L2 内部。
- [ ] **Step 2：写 4.2 L1** — 两个层次：**任务模式层**（从已有问题提取标准操作链，如设施选址＝Buffer/Intersect/Union/Erase）、**工具能力描述层**（输入类型→操作→输出类型 函数签名，平台无关：抽象 `buffer` → PyQGIS `native:buffer` / ArcPy `Buffer_analysis`）；存储 Neo4j 或 JSON/YAML；模式节点作高层指导、工具能力节点作原子单元支撑泛化。
- [ ] **Step 3：验证** — 对应 RQ1/创新4；术语统一；无跳步（模式层与工具层的关系有过渡句）。
- [ ] **Step 4：提交** `git commit -m "docs: 重构稿第四章 4.1总体设计+4.2规划知识层"`

---

## Task 3b：第四章 4.3 L2 任务规划层（两步法 · 全文核心）

**Files:** Modify `proposal_v2.md`（填充 4.3.1/4.3.2/4.3.3）
**素材来源：** `proposal.md:79-126`（DAG 建模、两阶段生成、networkx 验证器）。**4.3.1 语义约束层为新增（一等公民）。**

- [ ] **Step 1：写 4.3.1 NL→空间语义约束表示（新）**

先写一段动机（承接 1.4/1.5：语义约束是"可被机器映射的结构"的载体）。再给出 schema 的 7 要素＋隐含语义操作化，并附与设施选址任务一致的实例（自然语言："在福田区内筛选距学校 500m 内、距工厂 1000m 外、位于商业用地的区域"）：

```json
{
  "task_id": "TASK_015",
  "analysis_goal": "facility_siting",
  "spatial_objects": [
    {"role": "study_area",   "type": "Polygon", "source": "futian.gpkg:district_boundary"},
    {"role": "near_features","type": "Point",   "source": "futian.gpkg:schools"},
    {"role": "avoid_features","type": "Point",  "source": "futian.gpkg:factories"},
    {"role": "landuse_layer","type": "Polygon", "source": "futian.gpkg:landuse"}
  ],
  "spatial_relations": ["proximity", "containment"],
  "positive_constraints": [
    {"object": "near_features", "relation": "within_distance", "value": 500, "unit": "m"}
  ],
  "negative_constraints": [
    {"object": "avoid_features", "relation": "beyond_distance", "value": 1000, "unit": "m"}
  ],
  "attribute_conditions": [
    {"layer": "landuse_layer", "field": "landuse_type", "op": "in", "value": ["商业用地","商住混合用地"]}
  ],
  "output_requirement": {"type": "Polygon", "semantics": "candidate_regions"},
  "implicit_operationalization": [
    "如出现‘交通便利’等隐含语义，操作化为 距路网<阈值 的缓冲区相交"
  ]
}
```

- [ ] **Step 2：写 4.3.2 语义约束→DAG** — 节点字段（task_id/description/tool_primitive/input_schema/output_schema）、数据流边、global_constraints；放 `proposal.md:94-124` 的 DAG JSON 示例（承接上面的语义约束实例化而来）；两阶段约束生成（粗：子任务序列；细：声明 I/O 类型与跨任务依赖）＋硬约束（所有输入须有上游供给、不可成环）。
- [ ] **Step 3：写 4.3.3 DAG 验证器** — 基于 `networkx`：无环性、输入输出类型匹配、约束满足性。
- [ ] **Step 4：验证** — 两步关系清晰（语义约束是 DAG 的上游、两者非同物）；语义约束实例与 DAG 实例为同一任务、可对照；对应 RQ2/创新1+2。
- [ ] **Step 5：提交** `git commit -m "docs: 重构稿第四章 4.3任务规划层（语义约束→DAG两步法）"`

---

## Task 3c：第四章 4.4 L3 执行代理层 + 4.5 L4 规划迭代优化层（反转核心）

**Files:** Modify `proposal_v2.md`（填充 4.4、4.5）
**素材来源：** `proposal.md:130-152`（执行捕获、Auditor 三步、修正知识积累）。

- [ ] **Step 1：写 4.4 L3** — 执行跟踪与错误捕获：异常信息（`ToolNotFoundError`/`TypeError`/`InputError`/`SequenceError`）、任务状态（成功/失败）、关键数据状态（大小/参考系等）。
- [ ] **Step 2：写 4.5 L4（反转叙事）** — 先点明反转论据：**正因 L2 产出了语义约束＋DAG 这一可映射结构，执行错误才能被精准回溯到规划层的具体缺陷**（语义约束缺失 / DAG 依赖缺失 / 工具抽象与平台实现不匹配）——这正是现有 Reviewer 后验修补做不到的。再写 Plan Auditor 三步（诊断＝基于诊断规则库判根源；定位＝在 DAG 标问题节点/边；修正＝生成修正指令 JSON 触发重规划），放 `proposal.md:144-151` 的修正指令 JSON 示例；最后写修正知识积累（"规划缺陷→修正方案"案例对存库作 few-shot）。
- [ ] **Step 3：验证** — 反转叙事到位（回溯能力显式归因于结构化表示）；对应 RQ3/创新3；与 1.4 对 Reviewer 的批判呼应而不矛盾。
- [ ] **Step 4：提交** `git commit -m "docs: 重构稿第四章 4.4执行层+4.5迭代优化层（错误回溯反转）"`

---

## Task 4a：第五章 5.1 数据 + 5.2 资源库 + 5.3 测试任务集

**Files:** Modify `proposal_v2.md`（填充 5.1–5.3）
**素材来源：** `proposal.md:160-167`（数据）、`proposal.md:229-381`（模式库/任务列表库/错误库）、`proposal.md:169-188`（T×R）。

- [ ] **Step 1：写 5.1 数据条件** — 放数据源表（OSM/轨迹/高德/水系，逐字取 `proposal.md:162-167`）。**全文数据只在此处定义一次。**
- [ ] **Step 2：写 5.2 资源库** — 5.2.1 任务模式库（放 `proposal.md:239-311` 的 PATTERN_006 JSON Schema）；5.2.2 任务列表库（放 `proposal.md:317-355` 的 TASK_015 实例 + expected_plan_dag 三步生成；**修正原稿笔误：TASK_015 的 `pattern_id` 应为 `PATTERN_006`，与其工作流一致，原稿误写 PATTERN_001**）；5.2.3 工具库（沿用现有工具体系，一句带过）；5.2.4 错误知识库（放 `proposal.md:365-381` 的 15 类 E01–E15 表，**新增一列 `mapped_planning_layer_defect`：把每类错误显式映射到 语义约束缺失/DAG依赖缺失/工具抽象不匹配** —— 落实 spec §8 待补1 默认值，支撑 4.5 反转）。
- [ ] **Step 3：写 5.3 测试任务集** — 放 R1–R4 等级表与 T×R 90 任务矩阵（逐字取 `proposal.md:173-188`）。
- [ ] **Step 4：验证** — 数据/模式库/任务库/错误库各仅一处；错误库新增映射列在位；T×R 合计＝90。
- [ ] **Step 5：提交** `git commit -m "docs: 重构稿第五章 5.1-5.3 数据/资源库/任务集"`

---

## Task 4b：第五章 5.4 评估指标 + 5.5 对比基线与消融（**去重关键**）

**Files:** Modify `proposal_v2.md`（填充 5.4、5.5）
**素材来源：** 合并 `proposal.md:190-198`（旧 5 指标）与 `proposal.md:385-419`（14 指标）→ 一套；合并 `proposal.md:200-207` 与 `proposal.md:421-430`（基线）→ 一份。

- [ ] **Step 1：写 5.4 统一指标体系（三组，去重）** — 过程指标（节点/边/参数/约束匹配，取 `proposal.md:391-396`）＋结果指标（完全正确/功能正确但顺序不同/缺省/冗余/参数错/工具错/完全错，取 `proposal.md:402-410`）＋全局指标（首次成功率/最终成功率/平均修正次数/诊断准确率，取 `proposal.md:414-419`）。**显式加一句**：旧"成文大纲 4.3"的 5 个摘要指标已并入本三组，全文指标以此处为准。
- [ ] **Step 2：写 5.5 基线与消融（一份）** — 表：Baseline1 原版（线性Plan+执行后校验）/ Baseline2 GeoAgent（Lin et al. 无规划层）/ Baseline3 GPT-4o 直生代码 / Our Method（语义约束+DAG+Plan Auditor）。**每条基线注明消融了哪个组件**：B1=无语义约束层与回溯修正；B2=无规划层；B3=无规划。
- [ ] **Step 3：验证** — 全文**仅此一处**指标定义、**仅此一处**基线表；与 spec §5 对齐表 RQ↔指标 完全对应。
- [ ] **Step 4：提交** `git commit -m "docs: 重构稿第五章 5.4指标+5.5基线（两套合一去重）"`

---

## Task 4c：第五章 5.6 实验日志 + 5.7 预期结果

**Files:** Modify `proposal_v2.md`（填充 5.6、5.7）
**素材来源：** `proposal.md:434-450`（日志 JSON）、`proposal.md:209-217`（预期）。

- [ ] **Step 1：写 5.6** — 放实验日志 JSON 格式（逐字取 `proposal.md:434-448`）。
- [ ] **Step 2：写 5.7** — 放复杂度分层预期表（`proposal.md:211-215`）＋一句：对 CRS 不匹配/依赖顺序错/I-O 类型不匹配类失败，结构化回溯定向修正应能恢复 60–80%。
- [ ] **Step 3：验证 + 提交** `git commit -m "docs: 重构稿第五章 5.6日志+5.7预期"`

---

## Task 5：第六章「创新点与预期成果」（5 点）

**Files:** Modify `proposal_v2.md`（填充「六」）
**素材来源：** `proposal.md:30-33` 重整为 5 点；地位见 spec §4「六」。

- [ ] **Step 1：写 5 个创新点**（各 1–2 句）：①面向任务规划的 DAG 显式依赖建模（解决输出不可计算、依赖丢失）；②**NL→空间语义约束的结构化表示（新·headline）**；③**基于结构化表示的执行错误可定位与定向修正（反转·headline）**；④GIS 任务规划知识增强体系（模式库＋工具依赖约束表）；⑤真实地理数据环境（PostGIS＋GPKG＋轨迹）验证。
- [ ] **Step 2：验证** — 5 点；②③为头牌且与 4.3/4.5 呼应；创新5 为横切项（锚定第五章），与 spec §5 例外说明一致。
- [ ] **Step 3：提交** `git commit -m "docs: 重构稿第六章 创新点与预期成果"`

---

## Task 6a：精读 8 篇文献，产出文献笔记（"三、现状"的事实来源）

**Files:** Create `docs/superpowers/notes/2026-06-09-lit-notes.md`
**素材来源：** `20260602_Papers/files/` 8 篇 PDF。**适合派子代理并行精读。**

- [ ] **Step 1：逐篇精读，每篇记录**：架构（单/多智能体、层次）｜有无独立规划层及其输出形式（文档 vs 结构化）｜错误处理方式（有无 Reviewer/后验修补）｜评测口径（任务数、指标、成功率）｜可引证点（正引/批判/基线，对应本文哪节）。8 篇：GeoGPT(6803)、GIS Copilot(11456)、Mansourian&Oucheikh(11649)、MiniGeoSolver(11864)、GeoCogent(11445)、GeoColab(11473)、GeoAgent(11449)、GeoJSON agents(11447)。
- [ ] **Step 2：专列 GeoAgent(Lin 2026) 三重身份证据** —— 作为标杆（多智能体代表）、问题源（Reviewer 失效）、对比基线（B2）的具体出处。
- [ ] **Step 3：验证** — 8 篇齐全、每篇五项要素完整、论断有原文页码/小节支撑（不杜撰）。
- [ ] **Step 4：提交** `git commit -m "docs: 8篇文献精读笔记（现状章事实来源）"`

---

## Task 6b：第三章「国内外研究现状」

**Files:** Modify `proposal_v2.md`（填充「三」）
**素材来源：** `proposal.md:43-51`（演进线提示）＋ Task 6a 笔记（事实）。**依赖 Task 6a。**

- [ ] **Step 1：写 3.1 GeoAI → 3.2 LLM4GIS → 3.3 Autonomous GIS** —— 3.3 含三条演进线（架构：原型→线性→单体→多智能体；工具：窄小→大全；规划层：一步到位→转嫁修正→中间流程分离但存在性/职责未定论），全部以笔记事实支撑。
- [ ] **Step 2：写 3.4 规划层与错误处理评述** —— 收束到本文切入点；点明 GeoAgent 三重身份；平稳过渡到第四章。
- [ ] **Step 3：验证** — 引用均来自笔记（不杜撰）；三条演进线在位；漏斗式收束、与第四章衔接有过渡句。
- [ ] **Step 4：提交** `git commit -m "docs: 重构稿第三章 国内外研究现状"`

---

## Task 7：全文一致性通审

**Files:** Modify `proposal_v2.md`（跨章修订）

- [ ] **Step 1：术语统一扫描** — 全文术语为 docx 体系；建术语对照，逐一核（无"自修正自验证"等旧措辞与新话语混用）。
- [ ] **Step 2：去重核对** — 第四章（方法）与第五章（实验）不重复定义指标/基线/数据；每概念仅一处权威定义。
- [ ] **Step 3：对齐表核对** — 逐章内容均可映射到 spec §5 的 RQ/方法/创新/指标；无法归属者删或移。
- [ ] **Step 4：写作约定核对** — 各章引出处有漏斗、抽象处有排比、连接处有 because→therefore 过渡、无跳步（重点查新写的衔接段）。
- [ ] **Step 5：参考文献** — 8 篇按统一格式列入「参考文献」，正文引用与列表一一对应。
- [ ] **Step 6：提交** `git commit -m "docs: 重构稿全文一致性通审（话语/去重/对齐/引用）"`

---

## 执行顺序与依赖

```
Task 1 → 2 → 3a → 3b → 3c → 4a → 4b → 4c → 5      （主线，可顺序执行）
Task 6a（独立，可最先并行）→ 6b（依赖 6a）
Task 7（最后，依赖全部）
```
建议：6a 文献精读最先并行启动（子代理）；主线写完 + 6b 完成后做 Task 7 收口。

## 自检（写完计划后）
- **spec 覆盖**：spec §4 六章/§5 对齐表/§7 搬迁映射/§8 待补默认值 均有对应任务（待补1→4a Step2；待补2→3b Step1；待补3→4a Step3；待补5→6a/6b）。✓
- **占位符**：RQ 措辞、七项排比、语义约束 JSON、错误库映射列等关键内容均已具体化，无 TBD。✓
- **一致性**：`tool_primitive`/`input_schema`/`output_schema` 字段、`facility_siting` 任务实例在 4.3.1↔4.3.2↔5.2 间一致。✓
